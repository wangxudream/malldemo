server:
  port: 8085
spring:
  application:
    name: mall-test
#  profiles:
#    active: dev
##test:
#  test: aaa
---
spring:
  profiles: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
      discovery:
        server-addr: localhost:8848

#        prefix: mall-test
---

