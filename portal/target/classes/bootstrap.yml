server:
  port: 8080
spring:
  application:
    name: mall-protal
  profiles:
    active: dev

---
spring:
  profiles: dev
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
      config:
        server-addr: http://localhost:8848
        file-extension: yml
        prefix: mall-protal
---


